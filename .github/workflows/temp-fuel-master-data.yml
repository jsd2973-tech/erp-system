name: Temp fuel master data
on:
  push:
    branches:
      - feat/fuel-master-data-20260914
permissions:
  contents: write
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/fuel-master-data-20260914
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Apply changes
        run: python3 scripts/fuel_master_data_patch.py
      - name: Build
        run: npm ci && npm run build
      - name: Commit
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/features/fuel/FuelManagement.tsx src/features/fuel/fuelManagement.css
          git commit -m "feat: add fuel master data management [skip ci]"
          git push origin HEAD:feat/fuel-master-data-20260914
